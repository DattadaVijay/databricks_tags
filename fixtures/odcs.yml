dataContractSpecification: "0.9.3"
id: bronze-orders-contract
info:
  title: Bronze Orders Contract
  version: 1.0.0
  description: "Verified ODCS contract for raw bronze order events in Databricks."

servers:
  dev:
    host: dbc-e3761da8-7d95.cloud.databricks.com
    type: databricks
    catalog: workspace
    schema: default

schema:
  type: table
  description: "The physical table structure for bronze_orders as verified via Databricks SQL."
  properties:
    event_type:
      type: string
      description: "The lifecycle event type (e.g., OrderCreated, OrderCancelled)."
    order_id:
      type: string
      description: "The unique business identifier for the order."
    customer_id:
      type: string
      description: "The unique identifier for the customer."
    order_ts:
      type: timestamp
      description: "The UTC timestamp when the event occurred."
    channel:
      type: string
      description: "The sales channel source (e.g., web, mobile)."